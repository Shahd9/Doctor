<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.msaproject.doctor">

    <uses-sdk tools:overrideLibrary="com.google.zxing.client.android" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ApplicationClass"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true">

        <activity android:name=".ui.appointments.appointment_view_edit.EditAppointmentActivity"/>
        <activity android:name=".ui.appointments.appointment_view_edit.AppointmentDetailsActivity" />
        <activity android:name=".ui.appointments.appointments_list.AppointmentsListActivity" />
        <activity android:name=".ui.patient_details.PatientDetailsActivity" />
        <activity android:name=".ui.edit_profile.EditProfileActivity" />
        <activity android:name=".ui.medical_files.MedicalFilesActivity" />


        <activity android:name=".ui.phone_auth.PhoneAuthActivity" />

        <activity
            android:name=".ui.onboarding.OnBoardingActivity"
            android:theme="@style/AppTheme.NoStatusBar" />
        <activity android:name=".ui.host.HostActivity" />
        <activity android:name=".ui.complete_register.CompleteRegisterActivity" />
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/AppTheme.NoStatusBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>